export const SERVICE_NAME = 'ELMO';

export const ROUTES = {
  MAIN: '/',
  WELCOME: '/welcome',
  SETTING: '/setting',
  TRAIN: '/train',
  TEST: '/test',
} as const;

export const QUERY_KEYS = {
  HEALTH: 'health',
} as const;
